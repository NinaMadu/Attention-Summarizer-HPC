# Attention-Summarizer-HPC
From-scratch self-attention based text summarizer in C. Starts with pure serial implementation using real GloVe embeddings, then adds MPI, OpenMP, and CUDA versions. Manual tokenization, vectorization, scaled dot-product self-attention, and attention-driven extractive summarization.
